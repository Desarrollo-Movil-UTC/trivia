<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.utc.apptrivia9a">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppTrivia9a">
        <activity android:name=".Editar_EliminarPreguntaActivity"></activity>
        <activity android:name=".RegistrarJugadorActivity" />
        <activity android:name=".GestionJugadoresActivity" />
        <activity android:name=".GestionPreguntasActivity" />
        <activity android:name=".ResultadosActivity" />
        <activity android:name=".Pregunta2Activity" />
        <activity android:name=".Pregunta1Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>